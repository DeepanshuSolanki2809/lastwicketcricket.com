{
  "name": "lastwicketcricket-site",
  "private": true,
  "type": "module",
  "description": "Optional dev tooling for LAST Wicket Cricket. The site itself is plain static HTML/CSS/JS and needs no build step or dependencies to deploy on GitHub Pages — this is only for running the smoke test locally.",
  "scripts": {
    "test": "node test/smoke-test.mjs"
  },
  "devDependencies": {
    "jsdom": "^25.0.0"
  }
}
